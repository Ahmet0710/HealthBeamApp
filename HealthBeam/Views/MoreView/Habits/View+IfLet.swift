import SwiftUI

extension View {
    @ViewBuilder
    func ifHasValue<T, Content: View>(_ value: T?, transform: (Self, T) -> Content) -> some View {
        if let value = value {
            transform(self, value)
        } else {
            self
        }
    }
}
